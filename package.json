{
  "name": "dp-trader",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted paper trading dashboard and local API for Alpaca-style workflows.",
  "type": "module",
  "scripts": {
    "start": "node apps/api/server.js",
    "dev": "node apps/api/server.js",
    "test": "node --test",
    "lint": "node scripts/lint.js",
    "build": "node scripts/build.js",
    "check": "npm run lint && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
